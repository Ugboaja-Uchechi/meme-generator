import React from "react"

const Meme = () => {
  return (
    <main>
      <form className="form">
        <input type="text" className="form-input" />
        <input type="text" className="form-input" />
        <button className="form-button">Generate a meme image</button>
      </form>
    </main>
    )
}

export default Meme;